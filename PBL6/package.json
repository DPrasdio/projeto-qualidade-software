{
  "name": "localeats-pbl6",
  "version": "1.0.0",
  "description": "Testes Unitários e TDD — LocalEats",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0",
    "typescript": "^5.4.5"
  }
}
